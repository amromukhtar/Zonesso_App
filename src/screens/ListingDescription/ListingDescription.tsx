import React from "react";
import { ListingDescriptionProps } from "./ListingDescriptionProps";
import { View } from "react-native";

export const ListingDescription: React.FC<ListingDescriptionProps> = ({ navigation }) => {
  return <View></View>;
};
